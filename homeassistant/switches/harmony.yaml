- platform: template
  switches:
    tv:
      friendly_name: TV
      unique_id: template_harmony_tv
      value_template: "{{ is_state('remote.living_room', 'on') }}"
      turn_on:
        - service: remote.send_command
          entity_id: remote.living_room
          data:
            device: Projector
            command: PowerOn
        - condition: state
          entity_id: remote.living_room
          state: 'off'
        - service: remote.turn_on
          entity_id: remote.living_room
          data:
            activity: 'Play Shield'
      turn_off:
        service: remote.turn_off
        entity_id: remote.living_room
