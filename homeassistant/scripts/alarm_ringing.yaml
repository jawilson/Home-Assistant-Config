alias: Alarm Ringing
sequence:
  - service: switch.turn_off
    entity_id: switch.bedroom_fluxer
  - service: notify.html5
    data_template:
      target: '{{ target }}'
    data:
      title: 'Good Morning!'
      message: 'Turning on your wakeup light'
      data:
        tag: 'wakeup'
        renotify: false
        actions:
          - action: 'alarm_ringing_cancel'
            title: 'Cancel'
        script: 'script.alarm_ringing'
  - service: light.turn_on
    data_template:
      entity_id: '{{ light }}'
    data:
      transition: 120
      color_temp: 450
      brightness: 140
